name: Build Geode Mod

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
      
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        config:
          - name: Android32
            os: ubuntu-latest
            target: Android32

    name: ${{ matrix.config.name }}
    runs-on: ${{ matrix.config.os }}

    steps:
      - uses: actions/checkout@v4

      # Nombre correcto de la acción de setup
      - name: Setup Geode SDK
        uses: geode-sdk/setup@v1
        with:
          sdk: v5.5.0

      - name: Build Mod
        uses: geode-sdk/build@v1
        with:
          target: ${{ matrix.config.target }}
          combine: true
          
